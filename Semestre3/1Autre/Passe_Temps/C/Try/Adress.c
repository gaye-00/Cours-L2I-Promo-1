#include <stdio.h>
#include <stdlib.h>

int main(void)
{
	int age=10;

	printf("La valeur de la variable age est %d\n",age);	
	printf("L'adress de la variable age est %p\n", &age);

	return 0;
}